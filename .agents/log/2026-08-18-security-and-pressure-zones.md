# Security letter and pressure-zone pass

- Read Fleet letter `20260811-0bd55b1d-e6556b` from `code/fleet` to the canonical project address `research-projects/4000-PIHTI/2025-pihtivacuum`.
- Recorded its two findings: the public literal Flask session-signing key permits forged roster sessions, and `debug=True` on `0.0.0.0` exposes the Werkzeug debugger to the network.
- Read the logged owner-context note `20260811-8e38c05f-0bc326`: login is a name roster, not a security boundary. Therefore logs and plotting views remain public; the debug exposure retains full urgency.
- The initial cold-start packet named this sibling checkout as `hardware/2024-interactive-diagram`, which led this session to query the wrong mailbox. queezz corrected the canonical identity live on 2026-08-18.
- Collected the original security letter as received. Recalled the redundant finding `20260818-c6e55d7e-6198be`.
- Posted Fleet inventory defect `20260818-7b5008c0-1860de`: the sibling checkout and canonical PIHTI checkout were not marked as duplicates, so orientation queried the wrong mailbox.
- Rotated the encrypted-user Fernet key into `%LOCALAPPDATA%\pihti-diagram\users.key`, verified all six encrypted accounts decrypt, and removed the obsolete repository-local key. No secret values were printed.
- Security changes keep debug loopback-only, bind to loopback by default, replace the public Flask signing literal with a persistent machine-private key, decrypt users in memory, remove unrestricted CORS, validate state updates, and constrain control-unit filenames to the configured directory.
- UI pass assigns nominal atmosphere/gas, plasma-vacuum, and QMS-vacuum colors to named SVG pipes and chamber surfaces. The legend explicitly states these are schematic domains, not live pressure or a safety interlock.
- Automated evidence: JavaScript syntax check clean; every pressure-map SVG id exists; `pytest` 7 passed; Python compilation and `git diff --check` passed; forbidden debug/CORS/signing-literal patterns absent.
- Rendered the exact SVG mapping to a 1200 px PNG with Inkscape and inspected it: atmosphere/gas is orange, plasma-side is blue, QMS-side is purple, and shared isolation/roughing manifolds remain neutral rather than claiming a pressure without live evidence.
- Live-DOM Perimeter Walk is not complete. The in-app browser runtime rejected its own trusted dependency during setup, and this application has no registered `lab` service (`pihti` is a different CAD duplicate-review tool). No owner service was started or touched.
