# Operator guides and attribution identity

- queezz clarified that this surface guides an operator but never controls apparatus. Operation buttons select a visual procedure: numbered circles are overlaid on existing SVG equipment and the same ordered steps appear in a context rail. No guide action calls a device mutation route.
- Added a Fleet two-rail landing layout without changing the authored diagram theme: diagram context and guide selection on the left, current steps and warnings on the right, with the existing SVG as the main content. The same rail DOM becomes keyboard-dismissible drawers below 1180 px.
- Added provisional `Vent Plasma` and `Vent QMS` guide data. Completion and the first unmet step are derived from the operator-entered diagram state; gauge and turbo/gate warnings are advisory. The final pressure verification remains manual. Recorded an open owner direction to correct exact physical order, names, and marker placement after use.
- Added the mutually exclusive `Membrane installed`, `Pipe open`, and `Boron deposition` diagram context. Changes require a selected operator, persist outside the package, and write a separate attribution log rather than contaminating binary component history.
- Restored the established PIHTI identity boundary from the earlier owner decision and `pihti-log`: operator choice labels writes and is not authentication or access control. Removed the password form and password check, made read-only History, Plot, and downloads public, and kept diagram annotation writes identity-gated. The existing encrypted registry is read only for account names.
- Operator selection is permanent for a 12-hour inactivity window and is refreshed by attributable diagram/context changes. Passive five-second state polling does not refresh it. Updated `AGENTS.md` and the README so policy, UI, and implementation agree.
- Released this working tree as version `0.3.0`, synchronized between package metadata, `pihti.__version__`, navbar, and `/version`.
- Ran Fleet's Perimeter Walk against a synthetic Lab-tracked scratch service on confirmed-owned loopback port 48933. At 1280×1000 and 1280×700, both rail tops measured exactly 58 px at 0/25/50/75/100% scroll, controls held stable addresses across guide selection, and horizontal overflow was zero. At 480×700, the desktop rails were hidden, drawer triggers were visible, left and right drawers opened from the same DOM, Escape and the in-drawer Close worked, and horizontal overflow was zero.
- Verified five plasma markers/five rail steps and four QMS markers/four steps, one state-derived current step, persisted Boron context, password-free operator selection, the expected gauge warning, full History diagram/calendar/timeline, top navigation, Back/Forward/reload, empty Plot state, logo navigation, and Switch operator. Browser console was clean.
- The first perimeter found that the friendly empty Plot page still requested a missing last plot as HTTP 404. Changed that ordinary empty state to HTTP 200 and repeated the full perimeter cleanly.
- Tests: 13 passed. `diagram.js`, `identity.js`, and `navbar.js` passed `node --check`; `git diff --check` passed. The scratch process tree stopped, port 48933 was free, scratch material was removed, and the existing owner PIHTI service on port 4186 remained running throughout. No Raspberry Pi page, live apparatus input, real control-unit data, real operator identity, or private credential was touched.
- The registered Flask compatibility shim now disables bytecode writes before importing the package, so the long-running Lab service does not recreate package cache directories in Dropbox. The importer's own root cache is removed after each controlled restart.
- Live connected-volume color remains pending the owner's stable `zone-*` SVG split. Plot instrument IDs, units, background subtraction, and provenance remain a later phase.
