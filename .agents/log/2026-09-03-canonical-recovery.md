# Canonical checkout recovery

- Confirmed that the working checkout was 32 commits behind its remote and that the absent History, calendar, full locked SVG, Plot missing-folder notice, and packaged versioning already existed upstream.
- Preserved every pre-recovery tracked and untracked change in `stash@{0}`, fast-forwarded `master` to `e15ddd9`, and did not reapply the rejected tan theme or pressure-line jacket experiment. The recovery stash remains available.
- Ported the useful security work into the canonical `src/pihti` package: machine-private session and Fernet keys, in-memory user decryption, login gates for History/log/plot/download routes, same-origin write checks, safe file selection, security headers, loopback defaults, and debug isolation.
- Restored the current encrypted user store from the recovery stash so it remains paired with the existing private machine key. No key, password, decrypted user record, cookie, control-unit data, or private path was logged.
- Released the repaired working state as application version `0.2.0`, synchronized between package metadata and `pihti.__version__`, displayed it in the navbar, and added `/version`. Added the root Flask compatibility shim required by the registered Fleet Lab service.
- Removed the contradictory Plot error shown when no data directory exists, removed the obsolete Plotly/jQuery CDN scripts, and removed the browser-wide unversioned plot cache that could display the wrong file's plot.
- Fleet perimeter: inspected the real application at 1280×720 through an isolated Lab-launched loopback instance with synthetic scratch state. Vacuum, Login, History, Plot, Logout, browser back/forward, and reload worked. History showed the full SVG, calendar, two timeline events, and `pointer-events: none`. Plot showed the missing-directory notice without controls or console errors. No horizontal overflow was present. The available browser surface could not emulate a second viewport, so mobile behavior was code-reviewed rather than falsely claimed as a live measurement.
- Gates: `pytest` 10 passed; all changed JavaScript passed `node --check`; `pip check` reported no broken requirements; `git diff --check` passed.
- Live connected-volume coloring and advisory gauge/TMP vent warnings remain intentionally unimplemented until the owner supplies stable `zone-*` SVG groups. This avoids inventing plumbing topology from drawing geometry.
- The temporary perimeter process and browser tabs were stopped and closed. A later misunderstanding briefly started the registered PIHTI service; it was immediately stopped after the owner clarified that the Codex desktop app, not PIHTI, had gone down.
