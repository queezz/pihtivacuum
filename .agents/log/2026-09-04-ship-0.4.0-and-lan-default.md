# Ship 0.4.0 and answer on the LAN by default

Continues `2026-09-04-fleet-grammar-and-roster-paused.md`. Session on the
work Windows PC; queezz live in chat: "RasPi deployment stays. RasPi serves
this diagram. And the served diagram is consumed on a laptop or a phone.
Finish, commit."

## The LAN misunderstanding, settled

Both sides were right about different code. The Raspberry Pi at `pihti:5000`
runs the pre-0.4.0 checkout, whose `app.run` bound `0.0.0.0`, so it has
always answered on the LAN. The 2026-08-18 security session made this
repository default to `127.0.0.1`, so deploying 0.4.0 to the Pi unchanged
would have taken the diagram off the LAN. Fleet order
`20260904-61666628-ea9a73` carried the answer ("ALWAYS LAN. It already
answers on the LAN. It is supposed to be used on a laptop, not on a serving
RasPi."), recorded as owner decision 2026-09-04:

- `cli.py` and `server.py` default `PIHTI_HOST` to `0.0.0.0`; `--host
  127.0.0.1` or `PIHTI_HOST` narrows a run; `PIHTI_PORT` now also feeds the
  CLI default. Debug stays loopback-only (guard unchanged).
- `AGENTS.md`, `README.md`, `.agents/ensemble-health.md` say so; the LAN
  question left `.agents/directions.md`; a test pins the default and the
  debug guard.
- `lab-cli/services.toml`: the `pihti-diagram` entry now runs `python -m
  pihti run` with no `--host`, so `lab pihti-diagram` follows the package
  default; `docs/services.md` row updated. Committed in lab-cli by this
  session under RULES.md §11's amend-your-own-entry protocol (only those two
  files staged; lab-cli's dirty `src/lab_cli/cli.py` untouched), note posted
  to `code/lab-cli`.
- `deploy/pihti.service` needs no change: `python -m pihti run` on the Pi
  now binds the LAN by default.

## Perimeter Walk — completed

Scratch `lab start pihti-diagram --port 48934 --host 127.0.0.1` under scratch
`LAB_CONFIG`/`LAB_RUNTIME_ROOT`/`LAB_LOG_ROOT`, with `PIHTI_DATA_ROOT`,
`PIHTI_CUDATA_DIRECTORY`, `PIHTI_SETTINGS_FILE`, `PIHTI_SESSION_KEY_FILE`,
`PIHTI_USERS_FILE` and `PIHTI_USERS_KEY_FILE` all pointed at the session
scratchpad (runtime data copied there, nothing written in Dropbox). Listener
confirmed by `OwningProcess`: the port was held by the venv launcher's child
whose parent was Lab's tracked PID. This also proved the amended lab entry.
Owner port 4186 empty before and after.

Steps left open by the paused session, now done in the real DOM at 1280×1000
and 1280×700 (the pane scales the viewport, so rows and days were pressed
through the DOM's own click handlers):

- Choosing a moment writes `/history?at=…`; choosing a day writes
  `/history?day=…`; both by `replaceState`, so Browser Back leaves History
  for the page the reader came from (Vacuum), and Forward restores the last
  address (`?day=2026-09-03`, its day pressed, 11 rows). Reload on
  `?at=2026-09-03 16:20:20` restored the day, the pressed row, the Selected
  moment card and GVU green. Pressing the History tab from inside a deep
  state returned to `/history` home. Plot → Back landed on History at scroll
  0.
- Rails at 76 px on History at 0/50/100 % scroll at 700 px height; no
  horizontal overflow; console clean throughout.
- Tired-reader and Teaching pass on Vacuum: structure alone answers "what
  first" (operator in the top bar, then Line configuration or an Operation
  on the left) and the right rail shows the current guide once; the
  prototype disclaimer renders once. Vent Plasma placed 5 markers, 5 steps
  and its alert.
- `lab stop` killed the tree; tracked PID and child both gone; 48934 free;
  scratch root marked `superseded`.

## Gates

`pytest` 20 passed (exit 0), `pip check` clean, `node --check` on all five
JS files, `git diff --check` clean.

## Mail and orders

- Order `20260904-61666628-ea9a73` (LAN) recorded in `AGENTS.md`, collected.
- Letter `20260903-ac0f4c69-3e864d` (captured-state permalink) answered by
  a note to `code/pihti-log` naming `/history?at=`, `/state.svg[?at=]`,
  `/history/state-at`, and the LAN default; collected.
- The ensemble health endpoint and tab remain open directions; the reply on
  that shape goes with `/api/health` when it lands.

## Still open

- The Pi still runs the old checkout. Updating it is a push (queezz's call)
  and a pull on the Pi. From this session's shells the Pi refused the key
  (no agent), so nothing there was inspected or changed.
- Vent Plasma / Vent QMS remain prototype: queezz has not yet given the
  hands-on sequence (gauges off, valve order, turbo before/after gate, where
  to verify pressure). `.agents/directions.md` still carries it.
- `users.json.enc` in this repository stays unopenable and superseded by the
  roster; removing it is queezz's call.
- `deploy/nginx-pihti.conf` says `server_name pihit.local` (typo); not
  touched, noted only.
