# Diagram repair and Fleet perimeter

- Read the attached Fleet cold start, `AGENTS.md`, `.agents/README.md`, the newest handoff, Fleet's `RULES-BRIEF.md`, `RULES.md` §§10–12, `WEBUI.md`, and `WEBUI-COOKBOOK.md` before changing the operator surface. The worktree already contained the prior security and nominal-pressure pass; none of it was staged, discarded, or committed here.
- Recorded Fleet's missing project purpose. Incorporated and collected owner order `20260818-c07c0ee9-b50872`: a future live-pressure view may use actual Raspberry Pi CSV readings, but must carry explicit instrument IDs, units, timestamps, and stale/unknown handling and may never infer safety from schematic color or pump state.
- Reproduced the line-color complaint in a live DOM. The prior CSS replaced every mapped SVG stroke with a uniform 5 px colored line and added unsupported glow to protected devices. This erased the SVG's authored 2–4 px hierarchy, visually broke the continuous black plumbing, and made non-live equipment look emphasized.
- Moved the nominal map into `static/pressureZones.json`. The browser now draws a low-opacity colored jacket behind each original black pipe, preserving the authored stroke, topology, interaction fill, and neutral/shared manifolds. Removed the equipment glow and added `Shared / valve-dependent` to the legend. A test now proves every mapped ID is unique and exists in the SVG.
- Replaced the mobile hamburger's clickable `div` and inline handler with a keyboard-operable button whose `aria-expanded` state follows the menu. Removed the redundant request that replaced the server-rendered navbar after load.
- Completed the Fleet Perimeter Walk through an isolated, marked, Lab-tracked scratch service on a confirmed spare loopback port. Browser control inspected the real DOM. At 1280×1000, 1280×700, and 480×700 there was no horizontal overflow; the heading stayed below the 50 px fixed navigation; all 34 original mapped paths retained black authored strokes and all 34 anonymous jackets rendered behind them. Mobile open/close worked from Enter and reported the correct expanded state.
- Pressed every top link from the changed surface. Vacuum returned home; Log and Plot reached their login-gated deep links; Login and PIHTI navigated correctly. Browser Back/Forward and reload preserved the expected deep-link states. This map has no rail or anchor jumps, so those two measurements were not applicable. An unauthenticated valve stayed `not-allowed` and did not change fill when pressed. A fresh tab produced no console warnings or errors. HTML, CSS, and JavaScript all returned `Cache-Control: no-store`.
- Stopped the scratch process tree with Lab, confirmed its port was free, confirmed the default owner port remained free, closed the temporary browser tabs, and marked the scratch root superseded. No control-unit data or credential material was opened.
- Gates: `pytest` 8 passed; `pip check` reported no broken requirements; both JavaScript files passed `node --check`; `git diff --check` passed.
- Plotting remains a later phase. The present code hard-codes `Ip_c`, `Pu_c`, `Pd_c`, and `Bu_c`, exposes raw channel codes with generic axis labels, parses no units or instrument identity, performs no background subtraction, and has no malformed/missing-channel error surface. It also loads `plotly-latest` from a CDN while generated Plotly HTML includes its own bundle, and caches one unversioned plot under a global IndexedDB key plus a server-side `last_plot.html`; those caches can show a plot for the wrong file or processing settings. The next plotting pass should first define a channel registry (operator name, hardware ID, unit, axis/scale), a background-selection and provenance model, and a cache key that includes file identity, processing recipe, and application version.
- Durable `lab pihti-diagram` registration is still absent from `lab-cli/services.toml`; this pass used an isolated temporary registry solely for verification. Posted Fleet letter `20260903-9cf5372e-bee78d` to `code/lab-cli` requesting the portable service entry through that repository's own workflow.
