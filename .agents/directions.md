# Open directions

- Name the valve you open to let air into the QMS vessel — the drawing has none on that vessel, and the guide will not point at a handle you have not named — the owner's call. `Vent QMS` now says to vent with air (the flow-calibration pipe is not plugged in), and that step is the one step in all four guides with no beacon on the drawing, because the volume map carries no vent valve on the QMS vessel: the vents it knows are on the gas panel, the two backing lines and the bypass pumping line. Say which one you actually open, or that the route is somewhere the drawing does not show, and the step gets its beacon.
  Stakes: an operator following the guide reaches the air step and has to know already which handle to turn.
  Recommendation: name the valve if it is on the drawing; if it is not, say so and it stays a step in words.
  Safe default: the step keeps its wording and its missing beacon, and says nothing it cannot prove.
  Done when: the step names a part with a `label`, or you have confirmed it should not.

- **Closed (owner action 2026-09-08, letter `20260908-c8ef7df5-5f5e40`).** The question was whether the plasma vessel carries an ionization gauge the drawing did not show. It does, and queezz answered it by moving the gauge in Inkscape rather than in words: *"moved the upstream IG to gauges manifold, where it is physically now for some time already."* Adopted in 0.15.0. The gauge whose id is `bypass-ionization-gauge` now hangs on the plasma vessel's own gauge manifold beside the single gauge and the upstream Baratron, so `Vent Plasma` step 1 and the gas-or-air press warning both name the right gauge and the right volume. **The bypass manifold is left with one gauge and no ionization gauge at all** — `bypass-absolute-gauge`, the Ulvac the two pump-down guides already send you to read.

- Say whether the moved ionization gauge should be **renamed in the drawing** — the owner's call. Its id is still `bypass-ionization-gauge` and it now reads the plasma vessel, so the id says one thing and the drawing another. Nothing is broken by it: the app reads the volume from `plumbing.json`, and the name a person sees was changed in 0.15.0 to "Upstream ionization gauge", your own word for it. An id is yours, so nothing was renamed without you.
  Stakes: only for whoever next opens `diagram.svg` in Inkscape and reads the object list. History would keep resolving the old id either way, the way the 0.11.1 renames do.
  Recommendation: rename it to `upstream-ionization-gauge` next time you have the file open, and a ship will follow it through the map, the guides and the alias table.
  Safe default: the id stays as it is and the app keeps saying "Upstream ionization gauge" to people.

- Read the 41 component names the diagram now shows a person — in the guide steps, the hover tooltip, the confirm box and the History timeline — and correct any that is not what you call that piece of equipment at the rig — owner work pending. They live as a `label` line beside each `id` in `src/pihti/static/elementsConfig.json`; nothing else needs touching. They were written for 0.10.0 by spelling the key out in words, and by borrowing the vent guides' own wording where it already named a part ("the plasma gate valve" for `GVU`, "the QMS ionization gauge" for `downstream-ionization-gauge`), with the rig designation kept in brackets where the key *is* the designation — "Plasma turbo pump (TMPU)". Nothing about the hardware was guessed beyond that. The ones most likely to be wrong, because only the upstream/downstream convention supports them: `GVBU`/`GVBD` ("Bypass gate valve, upstream/downstream"), `RoughU`/`RoughD` ("Roughing line, upstream/downstream"), `Rough-Bypass` ("Roughing bypass"), `valve_qms` ("QMS valve"), `bypass-l1`/`bypass-l2` ("Bypass line valve 1/2"), `bypass-vcr-u`/`bypass-vcr-d` ("Bypass VCR valve, upstream/downstream") and `upstream-single-gauge` ("Upstream single gauge" — kept because it reads like a Pfeiffer SingleGauge, not because anything here knows that). Three more joined in 0.11.0, the vent valves that were drawn but had no state until then: "Plasma backing line vent valve", "QMS backing line vent valve" and "Bypass pumping line vent valve".
  Done when: every label reads the way you would say it out loud at the rig, and `pytest` still passes.

- **Closed (owner decision 2026-09-08, letter `20260907-b9fddf7b-4768ce`).** queezz: "Right, you may and should correct spelling. And oddities. QMS is the QMS itself, an image. qms-vacuum is its pipe to the L valve." Applied in 0.11.1, in `diagram.svg`, `elementsConfig.json`, `plumbing.json`, and tests, with the render proved byte-identical before and after (same Inkscape PNG hashes as the 0.11.0 ship). `QMS` and `qms-vacuum` were kept exactly as they were, per the owner's own words above — not renamed to `qms-head` as an earlier draft of this item had guessed. History and the state file may still carry an old id from before this rename; `src/pihti/server.py`'s `ID_ALIASES` resolves it on read without rewriting stored history, proved by `tests/test_server.py::test_an_old_permalink_still_selects_the_renamed_element`. The rename table:

  | old | new |
  | --- | --- |
  | `bypas-manifold-downstream-line` | `bypass-manifold-downstream-line` |
  | `bypas-manifold-main` | `bypass-manifold-main` |
  | `bypas-manifold-t-downstream-t` | `bypass-manifold-t-downstream-t` |
  | `bypas-manifold-t-upsteram` | `bypass-manifold-t-upstream` |
  | `bypas-manifold-upstream-gv` | `bypass-manifold-upstream-gv` |
  | `bypas-manifold-upstream-t-to-pipe` | `bypass-manifold-upstream-t-to-pipe` |
  | `bypas-pumpline` | `bypass-pumpline` |
  | `bypas-pumpline-vent` | `bypass-pumpline-vent` |
  | `bypas-pumpline-vent-air-side` | `bypass-pumpline-vent-air-side` |
  | `plasma-vacuum-pump-portt` | `plasma-vacuum-pump-port` |
  | `gasapanel-manifold-argon` | `gaspanel-manifold-argon` |
  | `GVU-6` | `upstream-pumpline-vent-valve` |
  | `GVU-6-9` | `downstream-pumpline-vent-valve` |
  | `Rough-bypass-vent` | `bypass-pumpline-vent-valve` |
  | `nitrogen-line` | `nitrogen-bottle` |
  | `path1464-3-2-6` | `gasline-argon-1` (also moved into the `pipes-gasline-argon` group) |

  `Rough-Bypass` (the pump) kept its id, as asked. The letter also named an element whose id was reportedly `false`, to be renamed or dropped if stray; no such id exists in `diagram.svg` — the only `false` in the file is `showgrid="false"` in the Inkscape namedview, a false positive from an earlier session's own search. Nothing was done there, and nothing needs to be.

- **Closed (owner decision 2026-09-08, letter `20260907-dc825e44-e6d89e`).** queezz: "pipe open or membrane mode without a membrane are the same. Boron uses same pipe. But other end not connected to downstream. Could be pumped from plasma side via bypass. Membrane installed and the membrane 'valve' should be linked." Applied in 0.12.1: `line_configuration.modes.boron` in `plumbing.json` now names `dead_end_valve: bypass-vcr-d`, so under Boron deposition the downstream valve on the narrow pipe never reaches it, whatever position it is in — the pipe simply takes the plasma-vessel side's own predicted state, or isolated when that side is shut too, and it can still be pumped from the plasma side through the bypass manifold since that reach never depended on the pipe's far end. The drawn `Membrane` ellipse on the probe line is answered the same letter: `line_configuration.linked_valve` names it, `plumbing.predict()` reads it through the Line configuration on every state read rather than from a press, and `elementsConfig.json` marks it `followsLineMode` so it has no click of its own — closed only under *Membrane installed*, open under *Pipe open* and *Boron deposition* alike. `/update` refuses a direct write to it. Proved in `tests/test_server.py`: one test per configuration for the narrow pipe and the two vessels, one for the `Membrane` element following the configuration on every mode, and one that the readout never claims the QMS vessel for the plasma vessel under Boron deposition.

- Confirm two remaining plumbing guesses in `src/pihti/static/plumbing.json`, where the drawing alone could not settle it — the owner's call. The **helium bottle** is recorded as feeding the argon line, because that is where its stem is drawn; nothing here knows whether helium and argon really share that run. The **gas panel manifold** is one drawn line that runs straight through the nitrogen valve, so it takes a single colour along its whole length; if the nitrogen side should colour on its own, that line needs splitting in Inkscape at the valve.
  Stakes: each wrong guess paints a volume the wrong colour in one particular valve position, and the colour exists as a sanity check, so a wrong one is worse than none.
  Recommendation: the helium one is a yes-or-no; the gas panel one needs a few minutes in Inkscape.
  Safe default: both stay as recorded, and the key keeps saying the whole thing is a prediction.

- Judge the **seven colours** on your own screen and say whether any of them is wrong — owner work pending, and yours because colour is taste and this is your drawing. They were redrawn in 0.16.0 on your own amended brief — "design nice, not color blind nice", after you found gas and rough vacuum indistinguishable — so the six coloured ones now sit at nearly one lightness and are told apart by hue: a crimson for air, a magenta for gas, a blue for the plasma side, a green for the QMS side, an amber for rough vacuum, a teal for *pumped, sealed off*, and the one grey for *isolated*, which stays the quietest because it is the absence of a claim. The whole table, with every pair's distance, is in `.agents/log/2026-09-08-four-configurations-a-brighter-seven-and-pumps-that-say-so.md`. The legend chips are full swatches now, and More shows the seven in a row at the top so you can compare them side by side.
  Done when: queezz has looked at the drawing in a few states and either said the palette is fine or named which colour is wrong and in which direction.

- **Closed (owner decision 2026-09-08, letters `20260908-b429ce4b-2ad897`, `20260908-520505af-9047d4` and `20260908-76e09ee2-e322b2`).** Which line configurations the card should offer. queezz: *"1. membrane installed. 2. pipe open, but the membrane probe in or bellows, which is vacuum wise the same. 3. blank, bellows not connected. 4. boron deposition sample holder installed. Are good and actual variants we run now."* Applied in 0.16.0: the card offers exactly those four, in that order, each with a one-line meaning in its More, all read from `plumbing.json` rather than typed into the page. **Blank** is vacuum-wise a membrane — the crossover is closed at the flange — and is drawn by painting the probe segment (`probe-pipe`, from the membrane position to the cross) in a blank-flange tone of its own, with no plug at the membrane position, because there is no probe mounted there. The segment you split off in Inkscape, `probe-pipe-cross-to-GVBD`, is in the map now and takes the colour of the side it is on; before this release it was drawn but unmapped, so nothing coloured it at all. A configuration recorded under an older spelling still resolves, through the map's own alias table.

- **Closed (owner decision 2026-09-08, letter `20260908-93fb84a6-5a69cf`).** What a stopped pump looks like. 0.16.0 made it yellow, on a relayed line that read "stays as drawn (yellow)"; the drawing's own fill is grey, and yellow was the app's own *on* colour, so the release turned the off signal into the on one. queezz, at once: *"No, no! Blue and yellow, yellow reads like on. Gray for off was lost. Why? WHY???"* Corrected in 0.16.1: a stopped pump carries no ink of the app's at all and keeps his grey, and the pumps have no operator-palette colours left.

- **Closed (owner decision 2026-09-08, letter `20260908-7a3c9ac3-bbd7aa`).** queezz read the deployed 0.13.0 and corrected the vent guides in his own words, so the map's proposal is superseded by what he actually does at the rig. On the plasma side: *"why do we have to touch 3 on the QMS? Answer: cause agents don't understand. We don't need. GVBU closed, and we are isolated. If it's open and membrane installed, bypass closed - isolated. But in that case best to close the GVBD, just in case. Also 3 the bypass rotary pump, irrelevant there. Valve to bypass pumping should be checked, yes. Also 2, we can just close the gate. No need to stop TMP. In that case the pumping down procedure changes, it must use the bypass. It's up for the operator to select."* And on the gas: *"Let Nitrogen in.. we are using the big manual needle valve, not massflows for venting. So the argon line must provide gas."* On the QMS side: *"same on the downstream. Double isolation of chambers, hahaha. Overkill. GVBD closed - isolated already. And venting the downstream is also better with N2, but the flow calibration pipe is currently disconnected. We can connect. Or we can vent air, cause downstream is usually pumped for a long time anyways and it has good base pressure."*

  Applied in 0.14.0. The QMS valve and the roughing bypass left both isolation steps; the plasma turbo is marked but never demanded; the valve to bypass pumping is its own checked step; nitrogen reaches the plasma vessel through the nitrogen valve, the gas panel argon valve, the argon gas line and the main gas line. Both isolation steps now read the *prediction* rather than a list of valves (`separates`), which is the "read the guides from the predicted state" item of 0.12.0 shipped in the same release. Whether the flow-calibration pipe is plugged in is a machine-local fact, `FLOW_CALIBRATION_PIPE_CONNECTED` in `settings.json`, default false. The guides lost their prototype flag. The one line he did not answer — whether the plasma vessel carries an ionization gauge the drawing does not show — is a separate item above.

- Say whether a **stopped pump should still be a wall** in the prediction — the owner's call, found while building the 0.13.0 warnings. Every pump in `plumbing.json` is a boundary: nothing ever passes through one, running or stopped. That is why venting the plasma backing line leaves the vessel above it blue, and it is also why pressing a backing-line vent valve raises no warning even with that turbo spinning — the map has no path from a foreline to the vessel above its pump, so the warning has nothing to warn about.
  Stakes: on the drawing, whether a vented backing line ever colours the vessel above it; in the new warnings, whether venting a backing line under a spinning turbo says anything at all. If a stopped turbo really does let air through to the vessel, the diagram is quietly optimistic today.
  Recommendation: leave a *running* turbo a wall — that is right, and it is what the colours were built on. The open question is only the stopped one, and a spinning turbo vented from its exhaust side, which is a real way to break a pump and which this map cannot currently see.
  Safe default: pumps stay walls in both states, and the backing-line vents stay quiet.

- PIHTI Log asks whether this page's maximum width should match theirs: theirs stops at 1920 pixels, this one at 1600, so on a wide screen their board spreads and this one does not — the owner's call. Their cap is your own decision of 2026-09-04, so they are not changing it on their own, and neither is this side.
  Stakes: only on a screen wider than 1600 pixels, and only for how much empty margin sits beside the content.
  Recommendation: match at 1920 if you read this on the wide monitor; otherwise leave it.
  Safe default: nothing changes; the two differ above 1600 pixels.

- Name the folder on the lab NAS that this diagram's runtime data (`logs.csv`, `elements_state.json`, `operators.json`, the operation-context files) is copied into, so the Pi can copy there on a daily timer — owner work pending. Automatic from the Pi is settled (owner decision 2026-09-07: "Sure. NAS it is. I'll point you there when I'm in the lab"), so only the address is missing; the Pi also needs the share reachable from it.
  Done when: queezz gives the NAS path and confirms the Pi can write to it, and a dated daily copy lands there without anyone pressing anything.

- Adapt a **second SVG for power and water** from one of your own paper or conference drawings — owner work pending, no build until it exists (your idea, 2026-09-08, letter `20260908-cbf115b3-448390`: *"I was thinking about second SVG with power and water.... But I have to adopt it from one of my paper/conference svgs first."*). A second state drawing beside the vacuum one — heaters, the lamp, the variacs, the plasma supplies, the QMS, the water circuits — driven by toggles the same way, so PIHTI Log's close-out could read a state instead of asking blind.
  Done when: queezz drops the adapted SVG in `local/` with named elements, the way he did for the vacuum drawing.

- **Closed (built in 0.17.0).** Sealed off became a remembered, dated state, and the five corrections beside it shipped with it. The six letters — `20260908-fe94c769-493d71`, `20260908-2c3d9837-37ab4a`, `20260908-425e4cdb-09c038` (superseded by the first two), `20260908-e2498698-5c9c50`, `20260908-8eaaa7a9-334955` and `20260908-e6ada507-7aa064` — are collected. The teal seventh colour is retired from the map; a volume with a running pump on it wears that pump's state now, so the stub between a shut gate and its spinning turbo is high vacuum in the turbo's own side colour; an isolated volume that remembers something wears that colour hatched and the readout says what and for how long, with `isolated` grey left for a volume with no memory at all. The memory lives under `_volumes` in `elements_state.json`, is replayable from `logs.csv`, and is a plain argument to `predict()` so the practice mode can run the same predictor over its own copy. Stopped pumps recede rim and all, gas marks are sized to the vessel they sit in, and H₂/O₂/N₂ carry a true subscript. See `.agents/log/2026-09-08-sealed-is-a-memory-with-a-date-on-it.md`.

- Say whether the **hatch is too much of the drawing** when the rig is sitting shut down — owner work pending, and yours because this is colour and colour is taste. Every isolated volume that remembers something now wears its remembered colour hatched, which is what your letter asks for; the consequence nobody had seen until it was built is that a fully shut-down rig goes from almost entirely one quiet grey to almost entirely hatched colour — 47 of 113 painted shapes in the frame this session measured. It is right, and it may still be louder than you want.
  Done when: queezz has looked at the drawing with everything shut and either said it is fine, or named what should stay grey — for instance only the two vessels and the tees remembering, with plain pipes staying quiet.

- Say whether the **sealed hatch is the right marking, or something quieter is** — the owner's call. Your letter offered "a hatched or lighter treatment the ship chooses"; the hatch was chosen because lightening the six colours by even 20 per cent toward white drops two of them below the palette's own 3:1 floor against the stone field (the QMS green to 2.42, rough vacuum to 2.38, measured). The stripes are the drawing's own ground cut through the colour at 45 degrees, 5 units of ground in every 12, set in `plumbing.json` as `sealed_stripe` and `sealed_period`.
  Stakes: only how a shut volume reads at a glance. Nothing about what the app claims changes either way.
  Recommendation: live with it for a session before changing it; if it is too busy, the two numbers in the map are the dial — a thinner stripe in a longer period is a quieter hatch.
  Safe default: the hatch stays as built.
